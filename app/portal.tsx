"use client";
import {useMemo,useState} from 'react';import {sessions,attendance} from './data';
import {AssessmentForm,FeedbackForm} from './native-forms';
type S='home'|'module1'|'module2'|'module3'|'feedback'|'assessment'|'sessions'|'attendance'|'announcements';
const ghBase=typeof window!=='undefined'&&window.location.hostname.endsWith('github.io')?'/veaiskilling':'';
const path=(value:string)=>`${ghBase}${value}`;
const trans:any={en:['An initiative of Vision Empower Trust','Learn. Apply. Include.','Explore the modules','View programme tracker'],hi:['विजन एम्पावर ट्रस्ट की पहल','सीखें। अपनाएँ। समावेश करें।','मॉड्यूल देखें','कार्यक्रम ट्रैकर देखें'],kn:['ವಿಷನ್ ಎಂಪವರ್ ಟ್ರಸ್ಟ್ ಉಪಕ್ರಮ','ಕಲಿಯಿರಿ. ಅನ್ವಯಿಸಿ. ಒಳಗೊಳ್ಳಿ.','ಮಾಡ್ಯೂಲ್‌ಗಳನ್ನು ನೋಡಿ','ಕಾರ್ಯಕ್ರಮ ಟ್ರ್ಯಾಕರ್'],ta:['விஷன் எம்பவர் டிரஸ்ட் முன்முயற்சி','கற்றிடு. பயன்படுத்து. உள்ளடக்கு.','தொகுதிகளை காண்க','நிகழ்ச்சி கண்காணிப்பு'],bn:['ভিশন এমপাওয়ার ট্রাস্টের উদ্যোগ','শিখুন। প্রয়োগ করুন। অন্তর্ভুক্ত করুন।','মডিউল দেখুন','প্রোগ্রাম ট্র্যাকার']};
const modules:any={module1:['01','Understanding AI','Foundations','Build a clear mental model of AI, machine learning and generative AI—alongside their capabilities, limitations, bias and ethical use.','AI Skilling Module 1'],module2:['02','AI for Teachers','Classroom practice','Use AI to translate, explain, create worksheets, adapt activities and prepare accessible learning materials—with the teacher always in the loop.','AI Skilling Module 2'],module3:['03','AI for Visual Inclusion','Accessibility','Explore Seeing AI, Be My AI, OCR and other tools that turn visual information into speech, text and accessible formats.','AI Skilling Module 3']};
function Header({lang,setLang}:any){return <><a className="skip" href="#main">Skip to content</a><div className="top"><span>Inclusive AI skilling for educators and the VI community</span><label>Language&nbsp; <select value={lang} onChange={e=>setLang(e.target.value)}><option value="en">English</option><option value="hi">हिन्दी</option><option value="kn">ಕನ್ನಡ</option><option value="ta">தமிழ்</option><option value="bn">বাংলা</option></select></label></div><header className="header"><a href={path('/')} className="brand"><span className="logo">AI</span><span><b>AI Shiksha</b><small>Vision Empower Trust</small></span></a><nav><a href={path('/')}>Home</a><a href={path('/module-1')}>Modules</a><a href={path('/feedback')}>Feedback</a><a href={path('/assessment')}>Assessment</a><a href={path('/sessions')}>Sessions</a><a href={path('/attendance')}>Attendance</a><a href={path('/announcements')}>Announcements</a></nav></header></>}
function Footer(){return <footer className="footer"><div className="footerin"><div><h2>AI Shiksha</h2><p>Responsible, informed and conscious use of AI for inclusive education.</p></div><div><b>Programme partners</b><p>Vision Empower Trust<br/>IIIT Bangalore<br/>Microsoft</p></div><div><b>Resources</b><p><a href={path('/module-1')}>Learning modules</a><br/><a href={path('/sessions')}>Training tracker</a><br/><a href={path('/feedback')}>Share feedback</a></p></div></div></footer>}
function Home({t}:any){let total=sessions.reduce((a:any,x:any)=>a+(parseInt(x['Number participants'])||0),0);let states=new Set(sessions.map((x:any)=>x.State).filter(Boolean)).size;return <main id="main"><section className="hero"><div><div className="eyebrow">{t[0]}</div><h1>{t[1]}</h1><p>Practical AI skilling that helps educators create accessible learning—and helps people with visual impairment access the world with greater independence.</p><div className="actions"><a className="btn primary" href={path('/module-1')}>{t[2]} →</a><a className="btn" href={path('/sessions')}>{t[3]}</a></div></div><aside className="herobox"><div><b>03</b>learning modules</div><div><b>{sessions.length}</b>sessions documented</div><div><b>{attendance.length}+</b>teacher records</div></aside></section><section className="wrap"><div className="stats"><div className="stat"><b>{sessions.length}</b><span>training sessions</span></div><div className="stat"><b>{total}</b><span>participant touchpoints</span></div><div className="stat"><b>{states}</b><span>states reached</span></div><div className="stat"><b>3</b><span>practice-led modules</span></div></div></section><section className="wrap"><span className="pill" style={{color:'#2449af'}}>THE LEARNING JOURNEY</span><h2 className="title" style={{fontSize:'42px'}}>From understanding AI to using it inclusively</h2><p className="lead">Each 1.5–2 hour module combines demonstrations, discussion and hands-on practice.</p><div className="cards">{Object.keys(modules).map(k=>{let m=modules[k];return <a className="card" key={k} href={path(`/${k.replace('module','module-')}`)}><span className="pill">MODULE {m[0]}</span><h3>{m[1]}</h3><p>{m[3]}</p><b style={{color:'#2449af'}}>Open module →</b></a>})}</div></section><section style={{background:'#fff8ef'}}><div className="wrap"><span className="pill" style={{color:'#2449af'}}>WHY THIS MATTERS</span><h2 className="title" style={{fontSize:'42px'}}>AI can widen access—when people guide it well.</h2><p className="lead">The programme builds confidence without hiding the risks. Teachers learn to pause, check, verify and decide before using AI output.</p></div></section></main>}
function Module({id}:any){let m=modules[id];return <main id="main"><section className="pagehead"><span className="pill">MODULE {m[0]} · {m[2]}</span><h1>{m[1]}</h1><p className="lead">{m[3]}</p></section><section className="wrap"><div className="module-tabs" aria-label="AI Skilling modules">{Object.keys(modules).map(k=>{let item=modules[k];return <a key={k} className={`module-tab ${k===id?'active':''}`} aria-current={k===id?'page':undefined} href={path(`/${k.replace('module','module-')}`)}><span>Module {item[0]}</span><b>{item[1]}</b></a>})}</div><div className="actions"><a className="btn primary" href={path(`/assets/${m[4]}.pptx`)} download>Download PowerPoint</a><a className="btn" href={path(`/assets/${m[4]}.pdf`)} target="_blank">Open full screen</a></div><iframe className="viewer" title={m[1]} src={`${path(`/assets/${m[4]}.pdf`)}#view=FitH`}/><p className="small">Use the viewer controls to move through slides, or download the original PowerPoint.</p></section></main>}
function Form({kind}:any){let feedback=kind==='feedback';return <main id="main"><section className="pagehead"><span className="pill">PARTICIPANT {kind}</span><h1>{feedback?'Help us improve':'Check your learning'}</h1><p className="lead">{feedback?'Tell us what worked, what could be better, and what support would help next.':'Complete the 15-question assessment across all three modules.'}</p></section><section className="wrap form-wrap">{feedback?<FeedbackForm/>:<AssessmentForm/>}</section></main>}
const sessionHeads=['Date','Conducted By','Module number','State','School','Participant Type','Number participants','Number VI participants'];
function Sessions(){let[q,setQ]=useState('');let[open,setOpen]=useState(false);let[local,setLocal]=useState<any[]>([]);let rows=useMemo(()=>[...local,...sessions].filter((x:any)=>JSON.stringify(x).toLowerCase().includes(q.toLowerCase())),[q,local]);function save(e:any){e.preventDefault();let item=Object.fromEntries(new FormData(e.currentTarget));setLocal([item,...local]);setOpen(false)}return <main id="main"><section className="pagehead"><span className="pill">LIVE PROGRAMME LOG</span><h1>Session Tracker</h1><p className="lead">Browse completed sessions and document new sessions with field media and testimonials.</p></section><section className="wrap"><div className="filters"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search state, school, facilitator…"/><button className="btn primary" onClick={()=>setOpen(!open)}>+ Add session</button></div>{open&&<form className="panel grid" onSubmit={save}>{sessionHeads.map(x=><label className="field" key={x}>{x}<input name={x} required/></label>)}<label className="field full">Overall session observation<textarea name="Overall Session observation" rows={4}/></label><label className="field full">Photos, videos or audio testimonials<input name="Media" type="file" accept="image/*,video/*,audio/*" multiple/></label><button className="btn primary">Save session</button></form>}<p className="small">Showing {rows.length} records</p><DataTable heads={sessionHeads} rows={rows}/></section></main>}
function DataTable({heads,rows}:any){return <div className="tablewrap"><table><thead><tr>{heads.map((h:string)=><th key={h}>{h}</th>)}</tr></thead><tbody>{rows.map((x:any,i:number)=><tr key={i}>{heads.map((h:string)=><td key={h}>{x[h]}</td>)}</tr>)}</tbody></table></div>}
function Attendance(){let[q,setQ]=useState('');let rows=attendance.filter((x:any)=>JSON.stringify(x).toLowerCase().includes(q.toLowerCase()));let heads=['Date','Conducted by','Module numbers','State','School','Participant Type','Pre-assessment Score','Post-assessment Score'];return <main id="main"><section className="pagehead"><span className="pill">ANONYMIZED TEACHER RECORDS</span><h1>Teacher Attendance</h1><p className="lead">Search anonymized attendance records and review participation and assessment scores. Personal contact details and names are not published.</p></section><section className="wrap"><div className="filters"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search school, state, facilitator…"/></div><p className="small">{rows.length} teacher attendance records</p><DataTable heads={heads} rows={rows}/></section></main>}
function Announcements(){return <main id="main"><section className="pagehead"><span className="pill">WHAT’S NEW</span><h1>Announcements</h1><p className="lead">Programme updates, learning opportunities and new resources.</p></section><section className="wrap">{[['18 SEP 2026','AI Shiksha learning portal is live','All modules, trackers, feedback and assessment are now available in one accessible place.'],['ONGOING','Share session stories','Facilitators can add photos, videos and audio testimonials through the Session Tracker.'],['RESOURCE','Pause – Check – Verify – Decide','Use this four-step routine when AI supports a high-impact decision.']].map(a=><article className="announcement" key={a[1]}><time>{a[0]}</time><div><h2>{a[1]}</h2><p>{a[2]}</p></div></article>)}</section></main>}
export default function Portal({section}:{section:S}){let[lang,setLang]=useState('en');let body=section==='home'?<Home t={trans[lang]}/>:section.startsWith('module')?<Module id={section}/>:section==='feedback'||section==='assessment'?<Form kind={section}/>:section==='sessions'?<Sessions/>:section==='attendance'?<Attendance/>:<Announcements/>;return <><Header lang={lang} setLang={setLang}/>{body}<Footer/></>}
